services:
  - type: web
    name: next-flask-app
    runtime: node
    buildCommand: npm install && npm run build
    startCommand: npm run start-with-flask
    envVars:
      - key: FLASK_PORT
        value: 5328
      - key: NEXT_PUBLIC_API_URL
        value: /api  # Proxy to Flask backend